using System;

class Program
{
  static void Main()
  {
    // Array array = new Array();
    // array.ArrayStore();

    // Declare declare = new Declare();
    // declare.DeclareArray();

    // Sum s = new Sum();
    // s.SumArray();

    // Number num = new Number();
    // num.MaxMinNumber();

    // Reverse reverse = new Reverse();
    // reverse.ReversedNumber();

    Search search = new Search();
    search.SearchNumber();
  }
}
